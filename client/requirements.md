## Packages
framer-motion | Complex animations and page transitions
recharts | Analytics charts for the dashboard
date-fns | Date formatting for tasks and logs
clsx | Conditional class merging
tailwind-merge | Tailwind class merging

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Integration: Chat endpoints are provided by Replit AI integration at /api/conversations
Integration: Auth endpoints at /api/login, /api/logout, /api/auth/user
